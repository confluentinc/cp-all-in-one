{
  "name": "source_charts",
  "config": {
    "connector.class": "com.mongodb.kafka.connect.MongoSourceConnector",
    "tasks.max": "2",
    "connection.uri": "mongodb+srv://test:test@atc.oa4fi.mongodb.net/test?retryWrites=true&w=majority&connectTimeoutMS=300000",
    "copy.existing": "true",
    "copy.existing.max.threads": "2",
    "copy.existing.queue.size": "160000",
    "database": "test",
    "collection": "foo",
    "publish.full.document.only": "true",
    "topic.prefix": "charts"
  }
}